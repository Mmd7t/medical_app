import 'package:flutter/material.dart';

class CalculateWaterPage extends StatelessWidget {
  static const String routeName = 'calculateWaterPage';
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
